# Numerical ODE Solver Suite

Python implementations of numerical methods for solving ordinary differential 
equations (ODEs), built for a numerical methods course. Includes Euler's method, 
Runge-Kutta (RK4), and [add others you actually implemented].

## What's inside
- `final_project.ipynb` — [one line on what this solves]
- `midterm_project.ipynb` — [one line on what this solves]

## Tech
Python, NumPy, Matplotlib

## How to run
1. Clone this repo
2. `pip install -r requirements.txt` (numpy, matplotlib, jupyter)
3. Open notebooks in Jupyter: `jupyter notebook`
